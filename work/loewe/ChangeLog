2001-09-07   <vino.crescini@tuxia.com>

	* browser/gtkEmbedBrowser.cpp:
	* browser/gtkEmbedBrowser.h:
	* browser/gtkEmbedPromptDialog.cpp:
	* browser/gtkEmbedPrompDialog.h:
	* browser/gtkEmbedDialogManager.cpp:
	* browser/gtkEmbedDialogManager.h:

	  - Fixed confirm, prompt (normal, password & username),
            and select dialogs.

	* browser/gtkEmbedPref.cpp:

	  - Modified GetBoolPref() to default to true if the
	    pref string is not in the file.

2001-09-06   <vino.crescini@tuxia.com>

	* browser/gtkEmbedParamList.cpp:
	* browser/gtkEmbedParamList.h:

	  - Added a list implementation to pass parameter to and and
	    from the dialogs.

2001-09-05   <vino.crescini@tuxia.com>

	* browser/gtkEmbedBrowser.cpp:
	* browser/gtkEmbedConst.h:
	* browser/gtkEmbedDialogManager.cpp:
	* browser/gtkEmbedDialogManager.h:
	* browser/gtkEmbedPromptDialog.cpp:
	* browser/gtkEmbedPromptDialog.h:

	  - Implemented YesNoDialog / YesNoDialogReply with
	    blocking.

	  - Modified dialog manager so that only one callback
	    function is required to be passed from gtkEmbedBrowser.

2001-09-04    <vino.crescini@tuxia.com>

	* browser/gtkEmbedConst.h:
	* browser/gtkEmbedDialogManager.cpp:
	* browser/gtkEmbedDialogManager.h:
	* browser/gtkEmbedPromptDialog.cpp:
	* browser/gtkEmbedPromptDialog.h:

	  - Modified dialog manager so that the callback functions 
	    do not need to be passed to the dialogs.

2001-08-31    <vino.crescini@tuxia.com>

	* browser/gtkEmbedBrowser.cpp:
	* browser/gtkEmbedPromptDialog.cpp:

	  - Implemented Alert dialog message.

2001-08-30    <vino.crescini@tuxia.com>

	* browser/gtkEmbedBrowser.cpp:
	* browser/gtkEmbedBrowser.h:

	  - Cleaned up main() and setGeometry() functions.

	  - Implemented enable/disable plugins. Only works with files
	    embedded within HTML pages.

2001-08-29    <vino.crescini@tuxia.com>

	* browser/gtkEmbedConst.h:
	* browser/gtkEmbedDialogManager.cpp:
	* browser/gtkEmbedHelperAppDialog.cpp:
	* browser/gtkEmbedHelperAppDialog.h:
	* browser/gtkEmbedPref.cpp:

	  - Modified helper app mechanism so that the mime/helper app
	    bindings are done through prefs.

	  - Fixed gtkEmbedPref::GetCharPref() so that it doesn't use
	    static var.

	  - Fixed gtkEmbedDialogManager::Init so that it now calls
	    the Init functions of the dialog classes.

2001-08-27    <vino.crescini@tuxia.com>

	* browser/gtkEmbedPrintManager.cpp:
	* browser/gtkEmbedPrintManager.h:
	* browser/Makefile:
	* browser/Makefile.in:
	* browser/gtkEmbedBrowser.cpp:
	* browser/gtkEmbedConst.h:

	  - Added a class that wraps the call to nsIWebBrowserPrint::Print().

	  - Sets nsIPrintOptions::SetPrintSilent to true to prevent the
	    print dialog from popping up.

2001-08-20    <vino.crescini@tuxia.com>

	* browser/gtkEmbedBrowser.cpp:
	* browser/gtkEmbedBrowser.h:

	  - Added a string check so that when a string is null,
            an empty string is sent to the socket.

	  - Fixed the outgoing messages functions so that it 
	    doesn't use the unsafe sprintf.

	  - Fixed the way shutdown is implemented.

	  - Fixed bug so that the browser will not crash when a 
	    new window with an onload=close() is opened.

	  - Now the geometry is set when a window is reopened 
	    (destroyed and recreated).

	* browser/Makefile:
	* browser/Makefile.in:

	  - Added __STRICT_ANSI__ to get rid of that ugly socket.h 
	    warning.

2001-08-14    <vino.crescini@tuxia.com>

	* browser/gtkEmbedConst.h:

	  - Moved all the constants to eliminate redundancies.

	* browser/gtkEmbedHelperAppDialog.h:
	* browser/gtkEmbedHelperAppDialog.cpp:

	  - Added class to override nsIHelperAppLauncherDialog. This
	    allows us to intercept calls to open an external application
	    for realplayer.

	  - When realplayer files are encountered (audio/x-pn-realaudio
	    and application/vnd.rn-realmedia) the application pointed
	    by the environment variable REALPLAYER_WRAPPER will be 
	    invoked.

2001-08-13    <vino.crescini@tuxia.com>

	* browser/gtkEmbedBrowser.h:
	* browser/gtkEmbedBrower.cpp:

	  - Added SSL active message.

	  - Window geometry and the last url loaded are now stored in prefs
	    so that they can be used at the next startup.

	* Makefile:
	* Makefile.in:
	* browser/gtkEmbedDialogManager.h:
	* browser/gtkEmbedDialogManager.cpp:
	* browser/gtkEmbedSSLDialog.h:
	* browser/gtkEmbedSSLDialog.cpp:
	* browser/gtkEmbedPromptDialog.h:
	* browser/gtkEmbedPromptDialog.cpp:
	* browser/gtkEmbedDialogConst.h:

	  - Files added to override nsIPromptService and nsINSSDialogs.
	    These components allows us to prevent the opening of alert,
	    prompt and confirm dialogs with all the SSL related dialogs.

2001-08-08    <vino.crescini@tuxia.com>

	* browser/gtkEmbedBrowser.h:
	* browser/gtkEmbedBrowser.cpp:

	  - Implemented changeFontSize, accepts either absolute or
	    relative changes.

	  - Added error codes for:
	    - invalid font size
	    - cant go back
	    - cant go forward
	    - cant reload
	    - cant stop
	    - general error
            - invalid find mode

	  - Implemented enable/disable cookies

          - Implemented find/find again

	  - Implemented purge cache

          - Reorganised #define's

	* browser/gtkEmbedIDManager.h:
	* browser/gtkEmbedIDManager.cpp:

	  - changed SetViews() to return true if successful, false otherwise

2001-08-07    <vino.crescini@tuxia.com>

	* browser/gtkEmbedPref.h:
	* browser/gtkEmbedPref.cpp:

	  - Added a wrapper class to set/get prefs.

	* Makefile:
	* Makefile.in:

	  - Added gtkEmbedPref.cpp

	* browser/gtkEmbedBrowser.cpp:

	  - Added enable/disable javascript support.
	  - Added enable/disable java support.
	  - Cleaned up shutdown code.
	  - New windows can now be opened. New windows will correctly
	    use the availableViews list.

2001-08-03    <vino.crescini@tuxia.com>

	* test/server.c:
        * browser/gtkEmbedBrowser.cpp:

	  - The server and the browser now reads and writes one character at 
            a time to/from the socket to prevent it from reading more than 
            one message at a time. Very inefficient, but correct.

            Of course, the socket listener still reads and writes in 1K blocks.

	* browser/gtkEmbedBrowser.cpp:
	* browser/gtkEmbedBrowser.h:
	* browser/gtkEmbedIDManager.cpp:
	* browser/gtkEmbedIDManager.h:

	  - Made a lot of change with the way the window management mechanism
	    works. Now, toggleView will switch between "used" windows. openURL
	    will load a url to an existing (used) window. If a window id is
	    specified, a window will only be opened if the given id is in
	    the available views list.

	  - The browser will now shutdown when the last "used" window is
	    destroyed.

2001-07-31    <vino.crescini@tuxia.com>

	* test/server.c:

	  - Cleaned up code
	  - Altered copy bytes function so that the case where the terminator
	    is \000 is still correctly handled.

	* browser/gtkEmbedBrowser.cpp:

	  - Modified code so that the terminator will still be copied
	    into the pipe even if it is the null character.

	  - Added wmclass string.

	  - enableForward/Back/Stop/Reload now sends the correct window
            id.

2001-07-30    <vino.crescini@tuxia.com>

	* browser/gtkEmbedSocketListener.h:
	* browser/gtkEmbedSocketListener.cpp:
	* browser/gtkEmbedBrowser.cpp:

	  - Converted SocketListener from an XPCOM object to a regular
	    cpp class.

	* browser/gtkEmbedIDManager.cpp:
	* browser/gtkEmbedIDManager.h:
	* browser/gtkEmbedBrowser.cpp:

	  - Modified gtkEmbedIDManager so that instead of using a constant
	    for the max id, the maximum is now passed as a parameter.

2001-07-27    <vino.crescini@tuxia.com>

	* ChangeLog

	  - ChangeLog started

	* browser/gtkEmbedBrowser.h:
	* browser/gtkEmbedBrowser.cpp:

	  - Implemented error messages
	  - Modified openURL to send out an error message if necessary.
	  - Implemented getURL

	* browser/gtkEmbedIDManager.h:
	* browser/gtkEmbedIDManager.cpp:

	  - Added isIDValid() function
