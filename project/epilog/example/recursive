/*
 * Recursive Example
 * Vino Crescini  <jcrescin@cit.uws.edu.au>
 */

/* identifier declarations */
ident sub-grp a,b,c;

/* initial state */
initially subst(a,b);
initially subst(b,c);
initially subst(c,a);

/* queries */
is subst(a,a);
is subst(a,b);
is subst(a,c);
is subst(b,a);
is subst(b,b);
is subst(b,c);
is subst(c,a);
is subst(c,b);
is subst(c,c);
