# Vino Crescini <jcrescin@cit.uws.edu.au>

TOPSRCDIR = @top_srcdir@
CC = @CC@
CXX = @CXX@
LEX = @LEX@
YACC = @YACC@
CFLAGS = @CFLAGS@
CXXFLAGS = @CXXFLAGS@
TARGETS = libcommon.a
INCLUDES = vlad.h list.h stringlist.h

all : public

public : $(TARGETS)
	@mkdir -p $(TOPSRCDIR)/include
	@mkdir -p $(TOPSRCDIR)/lib
	@for i in $(INCLUDES); do                  \
	ln -sf ../common/$$i $(TOPSRCDIR)/include; \
	done
	@for i in $(TARGETS); do               \
	ln -sf ../common/$$i $(TOPSRCDIR)/lib; \
	done

libcommon.a : list.o stringlist.o
	$(AR) -rcs $(@) list.o stringlist.o

list.o : list.h list.cpp
	$(CXX) $(CXXFLAGS) -o $(@) -c list.cpp

stringlist.o : stringlist.h stringlist.cpp list.o
	$(CXX) $(CXXFLAGS) -o $(@) -c stringlist.cpp

clean :
	$(RM) *.o

distclean : clean
	$(RM) $(TARGETS) Makefile
	@for i in $(INCLUDES); do       \
	$(RM) $(TOPSRCDIR)/include/$$i; \
	done
	@for i in $(TARGETS); do    \
	$(RM) $(TOPSRCDIR)/lib/$$i; \
	done
