TOPSRCDIR = @top_srcdir@
DISTDIR = @DISTDIR@
CURDIR = test
CC = @CC@
CFLAGS = @CFLAGS@
LDFLAGS = @LDFLAGS@
LN_S = @LN_S@
DEBUG = @DEBUG@
STRIP = @STRIP@
BUILDSHARED = @BUILDSHARED@
STATIC = @STATIC@
VERSION = @VERSION@
BINS =
STATIC_LIBS =
SHARED_LIBS =
INCLUDES =
OBJECTS =
TEMPS1 =
TEMPS2 =

ifdef DEBUG
  BINS += test
  OBJECTS += test.o
endif

prefix = @prefix@
exec_prefix = @exec_prefix@
bindir = @bindir@
libdir = @libdir@
includedir = @includedir@

include $(TOPSRCDIR)/rules.mk

test : $(OBJECTS)
ifdef STATIC
	$(CC) $(LDFLAGS) -static -o $(@) $(OBJECTS) -ltribe
else
	$(CC) $(LDFLAGS) -o $(@) $(OBJECTS) -ltribe
endif
ifndef DEBUG
  ifdef STRIP
	strip $(@)
  endif
endif

test.o : ../lib/rel.h ../lib/network.h test.c
	$(CC) $(CFLAGS) -o $(@) -c test.c
