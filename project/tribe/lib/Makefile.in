TOPSRCDIR = @top_srcdir@
DISTDIR = @DISTDIR@
CURDIR = lib
CC = @CC@
CFLAGS = @CFLAGS@
LDFLAGS = @LDFLAGS@
LN_S = @LN_S@
DEBUG = @DEBUG@
STRIP = @STRIP@
BUILDSHARED = @BUILDSHARED@
STATIC = @STATIC@
VERSION = @VERSION@
BINS =
STATIC_LIBS = libtribe.a
SHARED_LIBS = libtribe.so.$(VERSION)
INCLUDES = \
  config.h \
  tribe.h \
  mem.h \
  list.h \
  interval.h \
  rel.h \
  rqueue.h \
  rlist.h \
  network.h \
  $(NULL)
OBJECTS = \
  mem.o \
  list.o \
  rel.o \
  rqueue.o \
  rlist.o \
  network.o \
  $(NULL)
TEMPS1 =
TEMPS2 = \
  config.h \
  $(NULL)

ifdef BUILDSHARED
CFLAGS += -f$(BUILDSHARED)
endif

prefix = @prefix@
exec_prefix = @exec_prefix@
bindir = @bindir@
libdir = @libdir@
includedir = @includedir@

include $(TOPSRCDIR)/rules.mk

libtribe.a : $(OBJECTS)
	$(AR) -rcs $(@) $(OBJECTS)

libtribe.so.$(VERSION) : $(OBJECTS)
	$(CC) $(LDFLAGS) $(CFLAGS) -shared -Wl,-soname,`echo $(@) | cut -d. -f1,2,3` -o $(@) $(OBJECTS)
ifndef DEBUG
  ifdef STRIP
	strip $(@)
  endif
endif

mem.o : tribe.h mem.h mem.c
	$(CC) $(CFLAGS) -o $(@) -c mem.c

list.o : tribe.h mem.h list.h list.c
	$(CC) $(CFLAGS) -o $(@) -c list.c

rel.o : tribe.h interval.h rel.h rel.c
	$(CC) $(CFLAGS) -o $(@) -c rel.c

rqueue.o : tribe.h mem.h list.h rel.h rqueue.h rqueue.c
	$(CC) $(CFLAGS) -o $(@) -c rqueue.c

rlist.o : tribe.h mem.h list.h rel.h rlist.h rlist.c
	$(CC) $(CFLAGS) -o $(@) -c rlist.c

network.o : tribe.h mem.h list.h rel.h interval.h rqueue.h rlist.h network.h network.c
	$(CC) $(CFLAGS) -o $(@) -c network.c
